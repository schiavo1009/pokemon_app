import 'package:flutter/material.dart';

class ItemNotFoundedWidget extends StatelessWidget {
  const ItemNotFoundedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('No item founded'));
  }
}
